var canvas, backgroundImage;

var gameState = 0;
var playerCount;

var database;

var form, player, game;
var allPlayers;

function setup(){
  createCanvas(400,400);
form=new Form();
form.display();
}

function draw(){
}
